name: Docker - Publication

on:
  release:
    types: [published]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout du code
      uses: actions/checkout@v3
    
    - name: 🔐 Login à Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    
    - name: 🐳 Construire l'image
      run: |
        docker build -t ${{ secrets.DOCKER_USERNAME }}/tp-devops:latest .
        docker build -t ${{ secrets.DOCKER_USERNAME }}/tp-devops:${{ github.sha }} .
    
    - name: 🚀 Pousser l'image
      run: |
        docker push ${{ secrets.DOCKER_USERNAME }}/tp-devops:latest
        docker push ${{ secrets.DOCKER_USERNAME }}/tp-devops:${{ github.sha }}
    
    - name: 📦 Confirmation
      run: |
        echo "✅ Image Docker publiée !"
        echo "🏷 Tags : latest et ${{ github.sha }}"
        echo "📦 Disponible sur Docker Hub"